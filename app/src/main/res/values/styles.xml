<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@android:color/background_dark</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Light Theme-->
    <style name="AppTheme.Light" parent="AppTheme">
        <item name="customTextColor">@color/light_text</item>
        <item name="customBackground">@color/light_white</item>
        <item name="customButtonTextColor">@color/button_text_color</item>
        <item name="customCardBackground">@color/card_background_color</item>
    </style>
    <!-- Light High Contrast-->
    <style name="AppTheme.Light_HC" parent="AppTheme">
        <item name="customTextColor">@color/pure_black</item>
        <item name="customBackground">@color/pure_white</item>
        <item name="customButtonTextColor">@color/pure_black</item>
        <item name="customCardBackground">@color/pure_white</item>
    </style>
    <!-- Dark Theme-->
    <style name="AppTheme.Dark" parent="AppTheme">
        <item name="customTextColor">@color/night_text</item>
        <item name="customBackground">@color/night_black</item>
        <item name="customButtonTextColor">@color/button_text_color</item>
        <item name="customCardBackground">@color/card_background_color</item>
    </style>
    <!-- Dark High Contrast-->
    <style name="AppTheme.Dark_HC" parent="AppTheme">
        <item name="customTextColor">@color/pure_white</item>
        <item name="customBackground">@color/pure_black</item>
        <item name="customButtonTextColor">@color/pure_black</item>
        <item name="customCardBackground">@color/pure_white</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="MyDialogTheme" parent="Theme.AppCompat.Dialog.Alert">
        <item name="android:background">@color/background_color</item>
        <item name="android:textColor">@color/text_color</item>
        <item name="android:buttonBarNegativeButtonStyle">@style/NegativeButtonStyle</item>
        <item name="android:buttonBarPositiveButtonStyle">@style/PositiveButtonStyle</item>
        <item name="colorAccent">@color/mapboxPink</item>
    </style>
    <style name="NegativeButtonStyle" parent="Widget.MaterialComponents.Button.TextButton.Dialog">
        <item name="android:textColor">#f00</item>
    </style>

    <style name="PositiveButtonStyle" parent="Widget.MaterialComponents.Button.TextButton.Dialog">
        <item name="android:textColor">@color/text_color</item>
    </style>
</resources>
