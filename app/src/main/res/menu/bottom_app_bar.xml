<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- NEED TO ADD CORRECT BUTTONS TO EXIT MAPBOX NAVIGATION -->
    <!-- any other buttons? -->

    <item
        android:id="@+id/exit"
        android:icon="@drawable/mapbox_ic_exit_arrow_left"
        android:title="Exit"
        android:contentDescription="Exit nav"
        app:showAsAction="ifRoom"
        />

    <item
        android:id="@+id/more"
        android:title="More"
        android:contentDescription="More content"
        app:showAsAction="never"
        />
</menu>